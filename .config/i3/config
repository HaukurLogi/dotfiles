# vim: filetype=i3:et:ts=4:sts=4:sw=4:fdm=marker

# - Variables -
set $mod Mod1
set $term --no-startup-id alacritty
font pango:monospace 12

# #---Basic Definitions---# #

# - Border -
for_window [class="^.*"] border pixel 3

# - Setting refresh rate -
exec_always --no-startup-id xset r rate 250 30
exec xrandr --output DP-4 --mode 1920x1080 --rate 240.00
exec xrandr --output DP-0 --mode 2560x1440 --rate 144.00

# - External scripts -
# The --no-startup-id parameter disables startup-notification support for this
# particular exec command. With startup-notification, i3 can make sure that a
# window appears on the workspace on which you used the exec command. Also, it
# will change the X11 cursor to watch (a clock) while the application is
# launching. So, if an application is not startup-notification aware (most GTK
# and Qt using applications seem to be, though), you will end up with a watch
# cursor for 60 seconds.

# - Sound Switcher - 
exec_always --no-startup-id indicator-sound-switcher

# - Alacritty -
#exec_always --no-startup-id alacritty

# - Gaps -
#gaps inner 0
#gaps outer 0
#smart_gaps on

# - Wallpaper -
#exec_always --no-startup-id feh --bg-scale $HOME/Wallpaper/Retro.jpg
#exec_always --no-startup-id $HOME/bin/mpvbg $HOME/.bg/animated14.gif

# - Colours -
# class                 border  backgr. text    indicator child_border
client.focused          #141414 #222222 #e3e3e3 #292d2e   #141414
client.focused_inactive #141414 #222222 #e3e3e3 #292d2e   #141414
client.unfocused        #000000 #141414 #2b2b2b #292d2e   #0d0d0d
client.focused          #000000 #222222 #e3e3e3 #292d2e   #141414
client.focused_inactive #000000 #222222 #e3e3e3 #292d2e   #141414
client.unfocused        #000000 #141414 #2b2b2b #292d2e   #000000
client.urgent           #333333 #900000 #ffffff #900000   #333333
client.placeholder      #000000 #0c0c0c #ffffff #000000   #000000
client.background       #000000

# - Conky -
#exec_always --no-startup-id $HOME/.config/conky/conkystartup.sh

# - Transparent Alacritty -
exec_always --no-startup-id picom 

# - Start dunst for notifications -
exec --no-startup-id dunst

# - Keymapper - 
exec --no-startup-id key-mapper-control --command autoload

# - Unclutter -
exec --no-startup-id unclutter
# - Bluetooth Applet -
exec --no-startup-id blueman-applet

# - Network Manager applet -
exec_always --no-startup-id nm-applet

# - Polybar -
exec_always --no-startup-id $HOME/.config/polybar/launch.sh

# - Redshift is like f.lux -
exec --no-startup-id redshift

# - Lock the screen when AFK -
exec --no-startup-id $HOME/.config/i3/lock_daemon.sh

# - Behaviour -
# Yes focus windows with mouse
focus_follows_mouse yes

# - Bindings -
# -- Non-Letter key bindings -- {{{
bindsym $mod+Return              exec $term

# - Floating - 
bindsym $mod+m               floating toggle; [tiling con_id=__focused__] border none; [floating con_id=__focused__] border normal
default_border none
default_floating_border normal
bindsym $mod+Shift+Escape        exec prompt "Are you sure you want to leave i3?" "i3-msg exit"

# - Keyboard Shortcut -
exec --no-startup-id "setxkbmap -layout 'us,is' -option 'grp:alt_space_toggle'"

# - Screenshot -
bindsym $mod+s exec --no-startup-id maim --select "/home/$USER/$(date)"

# - Volume -
bindsym XF86AudioRaiseVolume      exec pactl set-sink-volume @DEFAULT_SINK@ +5%
bindsym XF86AudioLowerVolume      exec pactl set-sink-volume @DEFAULT_SINK@ -5%
#bindsym $mod+Up                  exec pactl set-sink-volume @DEFAULT_SINK@ +5%
#bindsym $mod+Down                exec pactl set-sink-volume @DEFAULT_SINK@ -5%

# - Pavucontrol -            
bindsym XF86MonBrightnessUp       exec xbacklight -inc 15
bindsym XF86MonBrightnessDown     exec xbacklight -dec 15
# }}}
# -- Letter key bindings -- {{{
# 'quit'
bindsym $mod+q                    kill
bindsym $mod+Shift+q              kill
# 'large'
bindsym $mod+f                    fullscreen toggle
bindsym $mod+u                    workspace prev
bindsym $mod+Shift+u              move container to workspace prev
#bindsym $mod+i                   focus up
bindsym $mod+i                    exec pactl set-sink-volume @DEFAULT_SINK@ +5%
#bindsym $mod+Shift+j             move left
#bindsym $mod+j                   focus left
bindsym $mod+Shift+k              move down
bindsym $mod+k                    exec pactl set-sink-volume @DEFAULT_SINK@ -5%
#bindsym $mod+l                   focus right
#bindsym $mod+Shift+l             move right
bindsym $mod+shift+b exec blueman-manager

# - Floating Window -
bindsym $mod+m                    floating toggle; [tiling con_id=__focused__] border none; [floating con_id=__focused__] border normal

bindsym $mod+o                    workspace next
bindsym $mod+Shift+o              move container to workspace next
bindsym $mod+d                    exec rofi -show drun fullscreen
#bindsym $mod+d                   exec rofi -show drun fullscreen

# - Tabbed mode -
bindsym $mod+t                   layout tabbed

# - Normal mode - 
bindsym $mod+n                   layout toggle split
bindsym $mod+h                   split horizontal
bindsym $mod+shift+x             exec --no-startup-id ~/.config/i3/lock.sh

# - Chromium -
bindsym $mod+c                    exec --no-startup-id chromium
bindsym $mod+v                   split vertical
bindsym $mod+comma               resize shrink height 10 px or 10ppt
bindsym $mod+Shift+r             exec i3-msg restart
bindsym $mod+Shift+comma         resize shrink width 10 px or 10ppt
bindsym $mod+period              resize grow height 10 px or 10ppt
bindsym $mod+Shift+period        resize grow width 10 px or 10ppt
 # }}}
# -- Workspace bindings -- {{{

# - All workspace bindings - 
bindsym $mod+Home                workspace $ws1
bindsym $mod+Shift+Home          move container to workspace $ws1
bindsym $mod+End                 workspace $ws10
bindsym $mod+Shift+End           move container to workspace $ws10
bindsym $mod+Tab                 workspace back_and_forth
# name workspaces
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"
# switch to workspace
bindsym $mod+1                  workspace $ws1
bindsym $mod+2                  workspace $ws2
bindsym $mod+3                  workspace $ws3
bindsym $mod+4                  workspace $ws4
bindsym $mod+5                  workspace $ws5
bindsym $mod+6                  workspace $ws6
bindsym $mod+7                  workspace $ws7
bindsym $mod+8                  workspace $ws8
bindsym $mod+9                  workspace $ws9
bindsym $mod+0                  workspace $ws10
# move focused container to workspace
bindsym $mod+Shift+1            move container to workspace $ws1
bindsym $mod+Shift+2            move container to workspace $ws2
bindsym $mod+Shift+3            move container to workspace $ws3
bindsym $mod+Shift+4            move container to workspace $ws4
bindsym $mod+Shift+5            move container to workspace $ws5
bindsym $mod+Shift+6            move container to workspace $ws6
bindsym $mod+Shift+7            move container to workspace $ws7
bindsym $mod+Shift+8            move container to workspace $ws8
bindsym $mod+Shift+9            move container to workspace $ws9
bindsym $mod+Shift+0            move container to workspace $ws10
# #---Arrow Keys---# #
#bindsym $mod+Lef               focus left
#bindsym $mod+Dow               focus down
#bindsym $mod+Up                focus up
#bindsym $mod+Right             focus right
bindsym $mod+Left               focus left
bindsym $mod+Down               focus down
bindsym $mod+Up                 focus up
bindsym $mod+Right              focus right
bindsym $mod+Shift+Left         move left
bindsym $mod+Shift+Down         move down
bindsym $mod+Shift+Up           move up
bindsym $mod+Shift+Right        move right

